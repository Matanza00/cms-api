// import express from "express";
// import DriverController from "../controllers/driverController";
// import schemaValidator from "../middleware/schemaValidator";

// const router = express.Router();

// router.post("/", schemaValidator("/create-driver"), DriverController.createOne);
// router.get("/company/:companyId", DriverController.getAll);
// router.get("/:id", DriverController.getOne);
// router.get("/id/:companyId", DriverController.getAllWithoutPagination);
// router.patch(
//   "/:id",
//   schemaValidator("/update-driver"),
//   DriverController.updateOne
// );
// router.delete("/:id", DriverController.softDelete);

// export default router;
